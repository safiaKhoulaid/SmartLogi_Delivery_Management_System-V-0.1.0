spring:
  datasource:
    url: jdbc:h2:mem:testdb;DB_CLOSE_DELAY=-1;DB_CLOSE_ON_EXIT=FALSE
    driver-class-name: org.h2.Driver
    username: sa
    password: ""

  jpa:
    hibernate:
      ddl-auto: create-drop
    show-sql: true
    database-platform: org.hibernate.dialect.H2Dialect # Kay-goul l Spring
    properties:
      hibernate:
        # 👇 HADI HIYA L-MOHIMA: Bach n-m7iw Postgres Dialect li jay mn main
        dialect: org.hibernate.dialect.H2Dialect
        format_sql: true

  # Désactiver Liquibase
  liquibase:
    enabled: false

  # Config Mail pour Test
  mail:
    host: disabled.test.host
    port: 3000
    username: test
    password: test
    properties:
      mail:
        smtp:
          auth: false
          starttls:
            enable: false

server:
  port: 8089